# 講義「跑掉」檢查報告

檢查範圍：ch1–ch17 的中英文 reveal.js 講義，共 **34 份、1386 張投影片**
檢查方式：無頭 Chromium 逐張實際渲染量測（投影片畫布 1050×700，`scrollable: false`），加上 HTML/qmd 原始碼靜態掃描
檢查日期：2026-07-20

---

## 結論先講

大部分講義是乾淨的。真正會讓學生看不到內容的問題集中在**一個地方：`ch15-zh`**，那一份有 5 張投影片的圖被切掉，其中最嚴重的一張整整少了 369px 的內容。其餘 16 章大致正常，只有零星小瑕疵。

沒有壞掉的圖片連結、沒有未渲染的 Markdown/LaTeX、沒有 R 執行錯誤訊息外漏。

---

## A. 內容真的被切掉（需要修，共 8 張）

`scrollable: false` 表示超出 700px 的內容不會有捲軸，而是**直接消失**。

| 講義 | 第幾張 | 標題 | 超出 | 嚴重度 |
|---|---|---|---|---|
| ch15-zh | 12 | `coord_trans()` | +369px | 嚴重 |
| ch15-zh | 10 | 改變形狀 | +366px | 嚴重 |
| ch15-zh | 13 | `coord_polar()` | +333px | 嚴重 |
| ch15-zh | 7 | 以 `coord_flip()` 翻轉 | +303px | 嚴重 |
| ch15-zh | 6 | （無標題，`base <- ggplot(mpg, ...)`）| +216px | 嚴重 |
| ch14-zh | 18 | （無標題，轉換大小美學）| +81px | 中等 |
| ch6 | 44 | Reading raster data with stars | +7px | 可忽略 |
| ch6 | 3 | Maps | +2px | 可忽略 |

### ch15-zh 的根本原因

這不是排版微調的問題，是**翻譯時把程式碼結構改掉了**。

英文版的寫法是「先指派、不輸出，換頁後再用 patchwork 併排」：

```r
a <- ggplot(mpg, aes(displ, cty)) + geom_point() + geom_smooth()
b <- ggplot(mpg, aes(cty, displ)) + geom_point() + geom_smooth()
c <- ggplot(mpg, aes(displ, cty)) + geom_point() + geom_smooth() + coord_flip()
```

```
---
```

```r
library(patchwork)
(a|b|c)
```

中文版把 `a <-`、`b <-`、`c <-` 的指派拿掉了，也刪掉了 `---` 分頁：

```r
ggplot(mpg, aes(displ, cty)) + geom_point() + geom_smooth()
ggplot(mpg, aes(cty, displ)) + geom_point() + geom_smooth()
ggplot(mpg, aes(displ, cty)) + geom_point() + geom_smooth() + coord_flip()
```

少了指派，每個 `ggplot()` 就**當場印出來**，三張圖直直往下疊在同一張投影片上，第三張（也就是 `coord_flip()` 這個整張投影片的重點）完全看不到。

量化差異：

| | ch15.qmd | ch15-zh.qmd |
|---|---|---|
| 程式碼區塊 | 12 個 | 8 個 |
| `---` 分頁 | 12 個 | 8 個 |
| patchwork `(a\|...)` 併排 | 5 處 | **0 處** |

12 個程式碼區塊逐一比對，**8 個全部結構不一致**。建議直接照英文版的分頁與 patchwork 寫法重寫 `ch15-zh.qmd`。

---

## B. 右緣溢出（9 張）——其中 8 張是假警報

初步量測顯示 9 張投影片有 4–13px 的右緣溢出。逐字元檢查「最右側**可見**字元」的位置後，發現其中 8 張**根本沒有東西被切到**：

| 講義 | 最右可見字元 | 判定 |
|---|---|---|
| ch15 第19張 | 1048px | 未超出 |
| ch9 第29張 | 1047px | 未超出 |
| ch8 第27張 | 1045px | 未超出 |
| ch10 第59張 | 1048px | 未超出 |
| ch14 第30張 | 1048px | 未超出 |
| ch13-zh 第32張 | 1044px | 未超出 |
| ch15-zh 第15張 | 1049px | 未超出 |
| **ch17 第22張** | **1054px** | **真的超出** |
| **ch17-zh 第22張** | **1054px** | **真的超出** |

原因是行內程式碼（`` `code` ``）帶有 `white-space: pre-wrap`，換行處會保留一個空白字元懸在邊界外。空白是看不見的，所以那 8 張不需要動。

真正超出的只有 ch17 中英文版第22張，原因是程式碼區塊裡有一行長達 105 字元；程式碼區塊不會自動換行，所以尾端被切掉。已把該行折成多行修正。

順帶掃過全部 34 份講義，程式碼行超過 100 字元的只有這一處。

---

## C. 中英文版結構不一致（5 章）

同一章的中英文版投影片張數對不起來，代表某一版少了或多了分頁：

| 章節 | 英文張數 | 中文張數 | `---` 差異 | 說明 |
|---|---|---|---|---|
| ch15 | 21 | 17 | ZH 少 4 個 | 見上，最嚴重 |
| ch16 | 27 | 25 | ZH **多** 2 個 | 分頁位置與英文版不同 |
| ch13 | 34 | 33 | ZH 少 1 個 | 兩張投影片被併成一張 |
| ch14 | 32 | 31 | ZH 少 1 個 | 同上 |
| ch17 | 32 | 32 | ZH 少 1 個 | 張數同但分頁位置不同 |

ch13、ch14、ch16 的中文版最後結束在「版權聲明」，英文版則多一張 `Acknowledgement`，可一併對齊。

ch1–ch12 的中英文版結構完全一致。

---

## D. 一個需要澄清的「假警報」

初次量測時有 458 張投影片顯示「內容壓到頁尾」，看起來像大規模跑版。實際查證後**不是問題**：

其中 397 張的內容底部都停在完全相同的 688px。這是 reveal.js 的 `r-stretch` 機制——Quarto 會自動把單張圖片放大或縮小到剛好填滿剩餘空間，所以內容自然就停在接近底部的位置。這 458 張裡有 98% 含 `r-stretch` 圖片，屬於設計上的正常行為。

（附帶一提：這也是為什麼不能只用「離線量測元素高度」來判斷。用那種方法時 ch9 被誤報 18 張溢出，實際逐張渲染後是 0 張——因為離線量測時 `r-stretch` 沒有生效，圖片維持原始大尺寸。本報告的數字全部來自逐張實際渲染。）

剩下 11 張沒有圖片、內容確實偏長但仍在邊界內的，可留意但不急著改：ch10/ch10-zh 第31張、ch11 第6張、ch2 第28張、ch6 第11張、ch6-zh 第3張、ch7/ch7-zh 第9及16張、ch9 第35張。

---

## E. 檢查了但沒發現問題的項目

- **圖片連結**：34 份講義全部 `img src` 皆指向存在的檔案，0 個壞圖
- **未渲染語法**：沒有殘留的 Markdown、LaTeX 或 Quarto 語法漏到畫面上
- **R 執行錯誤**：沒有 `Error in`、`object not found` 之類的訊息外漏
- **交叉引用**：沒有 `??` 未解析的引用
- **完全乾淨的章節**：ch2、ch3、ch7、ch11（中英文版皆無任何溢出）

唯一一則警告訊息是 ch2 的 `Removed 138 rows containing missing values`，那是 ggplot 正常的資料提示，教學上通常是刻意保留的。

---

---

## F. 已完成的修正（2026-07-20）

原始 `.qmd` 已備份於 `.qmd-backup-20260720/`。

| 檔案 | 修正內容 |
|---|---|
| `ch15/ch15-zh.qmd` | 5 處補回 `a <- / b <- / c <-` 變數指派、`---` 分頁與 `library(patchwork); (a\|b\|c)` 併排；地圖投影段補一個 `---`。投影片將從 17 張回到 21 張，與英文版一致 |
| `ch14/ch14-zh.qmd` | 補回 2 個 `---`（尺度轉換段），把擠在一張的「patchwork 圖 + 說明 + 指派 + 第二張 patchwork 圖」拆成 3 張 |
| `ch6/ch6.qmd` | 第3張：精簡一句贅詞讓清單收進邊界；第44張：`stars` 程式碼區塊前補一個 `---` |
| `ch17/ch17.qmd`、`ch17-zh.qmd` | 把 105 字元的 `theme(legend.background = ...)` 折成多行 |

修正後的靜態驗證結果：

- 程式碼圍籬（```` ``` ````）全部配對正確
- ch15-zh 的 5 個 patchwork 區塊，`a`/`b`/`c` **都在使用前已定義**（不會 render 失敗）
- ch15 中英文版的標題與分頁結構逐項比對，**完全一致**（ZH 僅多出中文字型的 `setup` 區塊）

### 還需要你做的事

我的環境沒有 R 與 Quarto，無法重新產生 HTML。請在專案資料夾執行：

```bash
quarto render ch15/ch15-zh.qmd
quarto render ch14/ch14-zh.qmd
quarto render ch6/ch6.qmd
quarto render ch17/ch17.qmd
quarto render ch17-zh.qmd   # 於 ch17 資料夾內
```

渲染完成後可以再跑一次量測確認溢出歸零。

### 未處理（等你決定）

C 節的中英文版結構不一致（ch13、ch14、ch16、ch17 的分頁位置與結尾投影片）尚未動，因為那會改到你現有的內容分頁方式，屬於教學編排的判斷。
